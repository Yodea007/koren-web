// Stub de route — tout le module libraires vit dans src/libraires/ (voir son README.md).
export { GET } from '@/libraires/api/tarif-pdf'

export const runtime = 'nodejs'
export const dynamic = 'force-dynamic'
